<%= form_for([@game, @guess]) do |f| %>

<% if @guess.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@guess.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @guess.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>


<p>
  <%= f.label :guess %><br>
  <%= f.text_field :guess %>
</p>
<p>
  <%= f.submit %>
</p>
<% end %>
